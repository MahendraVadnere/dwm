#####################
# Application keybindings #
#####################

super + x
	kitty

# rofi launchers
super + r
   	rofi -combi-modi window,drun,run -show combi -modi combi
   		
alt + F4
	bash ~/.local/bin/dwm-power-menu
	
# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd; notify-send 'sxhkd' 'Reloaded config'
		
#Volume Start	
alt + ctrl + Up
	bash ~/.local/bin/changevolume up
	
alt + ctrl + Down
	bash ~/.local/bin/changevolume down

alt + ctrl + m
	bash ~/.local/bin/changevolume mute
#Volume End
